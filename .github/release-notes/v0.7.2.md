## Qaid v0.7.2 — Table header

- **Added a "Table" / "الطاوله" header** at the top of the Sebeeta scoreboard, above the Target section.

### Verification

- `npm run typecheck` clean
- `npm test` 237/237 passing
- `npm run build` succeeds
- Verified live in English and Arabic
